<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="DetailActivityRepoName" parent="AppTheme">
        <item name="android:textColor">@color/maastricht_blue</item>
        <item name="android:textSize">@dimen/detail_activity_text_size</item>
    </style>

    <style name="DetailActivityLabelName" parent="AppTheme">
        <item name="android:textSize">@dimen/detail_activity_text_size</item>
    </style>

</resources>
